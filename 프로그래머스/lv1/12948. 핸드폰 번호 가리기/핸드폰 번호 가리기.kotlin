class Solution {
    fun solution(phone_number: String): String {
        var x :String = ""
        for (i in 0..phone_number.length-5){
            x += '*'
        }

        var answer = x+phone_number.substring(phone_number.length-4,phone_number.length)
        return answer
    }
}