class Solution {
    fun solution(seoul: Array<String>): String {
        var ans = ""
        seoul.mapIndexed{index, s ->
            when(s == "Kim"){
                true -> ans = "김서방은 ${index}에 있다"
            }
        }
        return ans
    }
}