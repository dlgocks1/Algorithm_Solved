class Solution {
    fun solution(s: String): Boolean {
        var len = s.filter {
            it.isDigit()
        }.length
        
        return if ((s.length == 4 || s.length == 6) && s.length == len) true else false
    }
}